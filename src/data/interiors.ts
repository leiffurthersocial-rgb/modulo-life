import type { InteriorDefinition, InteriorPropDef } from '@/game/types';
import { CHARACTERS } from './characters';

/**
 * Interiors are separate small scenes the engine builds on demand when the
 * player walks through a door. Local space is centred on the room, the exit
 * always sits on the +Z (south) wall.
 */

const p = (
  kind: string,
  x: number,
  z: number,
  extra: Partial<InteriorPropDef> = {},
): InteriorPropDef => ({ kind, x, z, ...extra });

function homeInterior(charId: string, name: string, accent: string, locationId: string): InteriorDefinition {
  return {
    id: `int_${locationId}`,
    location: locationId,
    name: `${name}'s Place`,
    width: 9,
    depth: 8,
    height: 3.2,
    floorColor: '#b99a72',
    wallColor: '#efe7da',
    accentColor: accent,
    lightColor: '#ffdcae',
    lightIntensity: 1.05,
    spawn: { x: 0, z: 5.6 },
    exit: { x: 0, z: 7.4 },
    props: [
      p('bed', -6, -5, { rot: 0, interaction: 'sleep', label: 'Sleep' }),
      p('nightstand', -3.4, -5.4),
      p('storage', -8, -1.4, { rot: Math.PI / 2, interaction: 'storage', label: 'Storage Chest' }),
      p('desk', 6.2, -5.2, { rot: Math.PI, interaction: 'study', label: 'Study' }),
      p('chair', 6.2, -3.4, { rot: 0 }),
      p('bookshelf', 8, -1, { rot: -Math.PI / 2 }),
      p('fridge', 8, 3.6, { rot: -Math.PI / 2, interaction: 'fridge', label: 'Fridge' }),
      p('kitchenCounter', 5.2, 5.4, { rot: Math.PI }),
      p('sofa', -5.6, 2.4, { rot: -Math.PI / 2, interaction: 'relax', label: 'Sit and relax' }),
      p('lowTable', -2.6, 2.4),
      p('tv', -8, 2.4, { rot: -Math.PI / 2, interaction: 'tv', label: 'Watch TV' }),
      p('rug', -4.2, 2.4),
      p('plant', 7.6, -7.4),
      p('poster', 0, -7.7, { rot: 0, data: { owner: charId } }),
      p('floorLamp', -8, -7.4),
      p('mirror', 3.2, -7.7),
      p('wardrobe', 1.2, -7, { interaction: 'wardrobe', label: 'Wardrobe' }),
    ],
  };
}

const homes: InteriorDefinition[] = CHARACTERS.map((c) =>
  homeInterior(c.id, c.name, c.themeColor, c.home),
);

export const INTERIORS: InteriorDefinition[] = [
  ...homes,

  {
    id: 'int_konbini',
    location: 'konbini',
    name: 'Modulo Mart',
    width: 11,
    depth: 8,
    height: 3.4,
    floorColor: '#dfe3e6',
    wallColor: '#f7f9fa',
    accentColor: '#2fa4d8',
    lightColor: '#f2f7ff',
    lightIntensity: 1.5,
    spawn: { x: 0, z: 5.8 },
    exit: { x: 0, z: 7.4 },
    music: 'konbini',
    props: [
      p('register', -7.5, -5.5, { rot: Math.PI / 2, interaction: 'shop:konbini', label: 'Browse the shelves' }),
      p('counter', -7.5, -3, { rot: Math.PI / 2 }),
      p('shelf', 0, -6.4, { rot: 0 }),
      p('shelf', 4, -6.4, { rot: 0 }),
      p('shelf', -3, -1.5, { rot: Math.PI / 2 }),
      p('shelf', 1.5, -1.5, { rot: Math.PI / 2 }),
      p('shelf', 6, -1.5, { rot: Math.PI / 2 }),
      p('fridgeWall', 9.6, -2, { rot: -Math.PI / 2, interaction: 'shop:konbini', label: 'Drinks cooler' }),
      p('magazineRack', -9.4, 2, { rot: Math.PI / 2 }),
      p('crateStack', 8, 5),
      p('jobBoard', -9.4, -6.6, { rot: Math.PI / 2, interaction: 'job:konbini_clerk', label: 'Take a shift' }),
      p('hotFoodCase', 4.5, 4.5, { rot: Math.PI }),
    ],
  },

  {
    id: 'int_cafe',
    location: 'cafe',
    name: 'Kotori Café',
    width: 9,
    depth: 8,
    height: 3.6,
    floorColor: '#7d5c42',
    wallColor: '#e9dcc8',
    accentColor: '#b5794a',
    lightColor: '#ffd7a0',
    lightIntensity: 1.15,
    spawn: { x: 0, z: 5.8 },
    exit: { x: 0, z: 7.4 },
    music: 'cafe',
    props: [
      p('cafeCounter', 0, -5.4, { rot: 0, interaction: 'shop:cafe', label: 'Order at the counter' }),
      p('espressoMachine', -2, -6.4),
      p('shelfBottles', 3.5, -6.8),
      p('table', -5, -1, { rot: 0 }),
      p('chair', -5, 0.6, { rot: 0 }),
      p('chair', -5, -2.6, { rot: Math.PI }),
      p('table', -5, 3.4),
      p('chair', -5, 5, { rot: 0 }),
      p('chair', -5, 1.8, { rot: Math.PI }),
      p('table', 5, -1),
      p('chair', 5, 0.6, { rot: 0 }),
      p('chair', 5, -2.6, { rot: Math.PI }),
      p('table', 5, 3.4, { interaction: 'relax', label: 'Sit by the window' }),
      p('chair', 5, 5, { rot: 0 }),
      p('plant', -7.6, -6.6),
      p('jobBoard', 7.6, -6.6, { rot: -Math.PI / 2, interaction: 'job:cafe_barista', label: 'Take a shift' }),
      p('poster', 0, -7.7),
    ],
  },

  {
    id: 'int_supermarket',
    location: 'supermarket',
    name: 'Sakura Foods',
    width: 13,
    depth: 11,
    height: 4.2,
    floorColor: '#e6e6e2',
    wallColor: '#f6f2ea',
    accentColor: '#e88aa8',
    lightColor: '#fbf8f2',
    lightIntensity: 1.45,
    spawn: { x: 0, z: 8.8 },
    exit: { x: 0, z: 10.4 },
    props: [
      p('register', -9, 7, { rot: Math.PI / 2, interaction: 'shop:supermarket', label: 'Shop' }),
      p('register', -9, 4, { rot: Math.PI / 2, interaction: 'shop:supermarket', label: 'Shop' }),
      p('shelf', -6, -6, { rot: Math.PI / 2 }),
      p('shelf', -6, -1, { rot: Math.PI / 2 }),
      p('shelf', -1, -6, { rot: Math.PI / 2 }),
      p('shelf', -1, -1, { rot: Math.PI / 2 }),
      p('shelf', 4, -6, { rot: Math.PI / 2 }),
      p('shelf', 4, -1, { rot: Math.PI / 2 }),
      p('produceStand', 9, -6),
      p('produceStand', 9, -2),
      p('fridgeWall', 11.6, 3, { rot: -Math.PI / 2, interaction: 'shop:supermarket', label: 'Chilled goods' }),
      p('crateStack', 9, 7),
      p('sellDesk', -11.6, -2, { rot: Math.PI / 2, interaction: 'sell:supermarket', label: 'Sell items' }),
    ],
  },

  {
    id: 'int_restaurant',
    location: 'restaurant',
    name: 'Ramen Ichiya',
    width: 9,
    depth: 7,
    height: 3.3,
    floorColor: '#4a3a30',
    wallColor: '#3a2b24',
    accentColor: '#e8b04b',
    lightColor: '#ffc774',
    lightIntensity: 1.0,
    spawn: { x: 0, z: 4.8 },
    exit: { x: 0, z: 6.4 },
    props: [
      p('ramenCounter', 0, -2.4, { rot: 0, interaction: 'shop:restaurant', label: 'Order' }),
      p('stove', -5, -5),
      p('stove', -2, -5),
      p('shelfBottles', 4, -5.6),
      p('stool', -5, 0.4),
      p('stool', -2.5, 0.4),
      p('stool', 0, 0.4, { interaction: 'eat:restaurant', label: 'Sit and eat' }),
      p('stool', 2.5, 0.4),
      p('stool', 5, 0.4),
      p('noren', 0, 6.2),
      p('lantern', -7.4, 3),
      p('lantern', 7.4, 3),
    ],
  },

  {
    id: 'int_gym',
    location: 'gym',
    name: 'Iron Pine Gym',
    width: 10,
    depth: 8.5,
    height: 4.2,
    floorColor: '#3b3f45',
    wallColor: '#cfd3d6',
    accentColor: '#3f7d5a',
    lightColor: '#eaf2f6',
    lightIntensity: 1.3,
    spawn: { x: 0, z: 6.4 },
    exit: { x: 0, z: 7.9 },
    props: [
      p('benchPress', -6, -4, { rot: 0, interaction: 'train:strength', label: 'Bench press' }),
      p('dumbbellRack', -8.4, 1, { rot: Math.PI / 2 }),
      p('treadmill', 5.5, -5, { rot: Math.PI, interaction: 'train:stamina', label: 'Treadmill' }),
      p('treadmill', 8, -5, { rot: Math.PI }),
      p('punchBag', 0, -5.5, { interaction: 'train:combat', label: 'Punching bag' }),
      p('matArea', 0, 2),
      p('mirror', -9.7, -1, { rot: Math.PI / 2 }),
      p('waterCooler', 8.6, 4, { rot: -Math.PI / 2, interaction: 'drink:water', label: 'Water cooler' }),
      p('jobBoard', -9.7, 4.5, { rot: Math.PI / 2, interaction: 'gym:membership', label: 'Notice board' }),
    ],
  },

  {
    id: 'int_office',
    location: 'office',
    name: 'Modulo Works — Floor 2',
    width: 12,
    depth: 10,
    height: 3.4,
    floorColor: '#6f7681',
    wallColor: '#e5e8ec',
    accentColor: '#4a6b8a',
    lightColor: '#f0f6ff',
    lightIntensity: 1.35,
    spawn: { x: 0, z: 7.8 },
    exit: { x: 0, z: 9.4 },
    props: [
      p('deskCluster', -5, -4),
      p('deskCluster', 5, -4),
      p('deskCluster', -5, 2),
      p('officeDesk', 5, 2, { rot: Math.PI, interaction: 'job:office_analyst', label: 'Start a shift' }),
      p('partition', 0, -1, { rot: 0 }),
      p('whiteboard', 0, -9.6),
      p('plant', -10.6, -8),
      p('plant', 10.6, -8),
      p('waterCooler', 10.6, 4, { rot: -Math.PI / 2, interaction: 'drink:water', label: 'Water cooler' }),
      p('locker', -10.6, 5, { rot: Math.PI / 2 }),
    ],
  },

  {
    id: 'int_arcade',
    location: 'arcade',
    name: 'Neon Alley',
    width: 11,
    depth: 9,
    height: 4,
    floorColor: '#1d1a26',
    wallColor: '#2a2233',
    accentColor: '#ff4d8d',
    lightColor: '#ff7ab8',
    lightIntensity: 0.9,
    spawn: { x: 0, z: 6.8 },
    exit: { x: 0, z: 8.4 },
    music: 'arcade',
    props: [
      p('arcadeCabinet', -8, -6, { rot: 0, interaction: 'minigame:arcade', label: 'Reflex Rush' }),
      p('arcadeCabinet', -5, -6, { rot: 0, interaction: 'minigame:arcade', label: 'Reflex Rush' }),
      p('arcadeCabinet', -2, -6, { rot: 0, interaction: 'minigame:cards', label: 'Card Duel' }),
      p('arcadeCabinet', 1, -6, { rot: 0, interaction: 'minigame:dice', label: 'Dice Corner' }),
      p('craneMachine', 5, -6, { rot: 0, interaction: 'minigame:crane', label: 'Crane game' }),
      p('arcadeCabinet', -8, 0, { rot: Math.PI, interaction: 'minigame:running', label: 'Street Dash' }),
      p('arcadeCabinet', -5, 0, { rot: Math.PI, interaction: 'minigame:strength', label: 'Power Meter' }),
      p('tokenMachine', 8.5, -2, { rot: -Math.PI / 2, interaction: 'arcade:tokens', label: 'Token machine' }),
      p('vending', 8.5, 3, { rot: -Math.PI / 2, interaction: 'vending', label: 'Vending machine' }),
      p('scoreBoard', 0, -8.6, { interaction: 'arcade:scores', label: 'High scores' }),
      p('neonStrip', 0, 0),
    ],
  },

  {
    id: 'int_basement',
    location: 'basement',
    name: "Erim's Basement",
    width: 9,
    depth: 7.5,
    height: 2.9,
    floorColor: '#2d2833',
    wallColor: '#241f2c',
    accentColor: '#c9a227',
    lightColor: '#ffcf7a',
    lightIntensity: 0.75,
    spawn: { x: 0, z: 5.4 },
    exit: { x: 0, z: 6.9 },
    music: 'basement',
    props: [
      p('feltTable', -3.5, -3, { rot: 0, interaction: 'minigame:cards', label: 'Card table' }),
      p('feltTable', 3.5, -3, { rot: 0, interaction: 'minigame:dice', label: 'Dice table' }),
      p('wheelStand', 0, -6.2, { interaction: 'minigame:wheel', label: 'Fortune wheel' }),
      p('coinTable', -6.6, 2, { rot: Math.PI / 2, interaction: 'minigame:coinflip', label: 'Coin flip' }),
      p('highlowTable', 6.6, 2, { rot: -Math.PI / 2, interaction: 'minigame:highlow', label: 'High / Low' }),
      p('safe', -7.6, -5.6),
      p('barCounter', 0, 3.4, { rot: 0, interaction: 'basement:bar', label: 'Bar' }),
      p('lamp', 0, 0),
      p('poster', -8.7, -1, { rot: Math.PI / 2 }),
      p('crateStack', 7.6, -6),
    ],
  },
];

export const INTERIOR_MAP: Record<string, InteriorDefinition> = Object.fromEntries(
  INTERIORS.map((i) => [i.id, i]),
);

export function getInterior(id: string): InteriorDefinition | undefined {
  return INTERIOR_MAP[id];
}
